import React from "react";
import "./Orders.css";

const Orders = () => {
	return (
		// firebase real-time db (firestore)
		<div className="orders">
			<h1>Your Orders</h1>
		</div>
	);
};

export default Orders;
